# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Environments
.venv/
venv/
env/
.env
.python-version

# Data — fetched with tools/fetch_upstream.sh, never redistributed (~90 MB)
data/raw/
data/ref/
# Compiled pack — rebuilt with tools/compile_pack.py (~114 MB)
data/pack/

# Reference engine — cloned and built by tools/setup_flybrain_reference.sh
work/
vendor/

# Run outputs
outputs/
*.npy
*.npz

# Editors / OS
.vscode/
.idea/
*.swp
*~
.DS_Store
._*
.Spotlight-V100
.Trashes

# graphify — keep the three shareable outputs, drop the working files
graphify-out/*
!graphify-out/graph.html
!graphify-out/GRAPH_REPORT.md
!graphify-out/graph.json

# Claude Code: settings.json is shared project config and stays tracked;
# only the personal overrides are ignored.
.claude/settings.local.json
